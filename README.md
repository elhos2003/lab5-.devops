# Multi-Repo Web App with Docker Compose

This setup runs a full-stack web application using:

- React frontend
- Spring Boot backend
- PostgreSQL database
- nginx reverse proxy

## Running the project

```bash
docker-compose up --build
